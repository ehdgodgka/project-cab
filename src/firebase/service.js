import { projectStorage, projectFirestore, timestamp } from '../firebase/config';

const deleteProject = (projectId, imageUrl) => {
  if (imageUrl) {
    // 연결된 스토리지 객체 삭제
  }
  // document 삭제
  //
};

const modifyProject = (projectId, imageUrl) => {
  //이미지 변경시 스토리지 객체 변경
  // document 변경
};
